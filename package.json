{
  "name": "@nevermindzzt/dsh-manager-plugin",
  "version": "0.1.4",
  "description": "Direct dsh-manager reverse tunnel plugin for DeepSeek Harness",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "node scripts/build.mjs && prettier --write lib/client.js",
    "format": "prettier --write src/*.js lib/*.js scripts/*.mjs",
    "check": "npm run build && prettier --check src/*.js lib/*.js scripts/*.mjs && node --check src/index.js && node --check src/protocol.js && node --check src/tunnel.js && node --check src/client.js && node --check lib/client.js",
    "test": "npm run check",
    "prepack": "npm run build"
  },
  "exports": {
    ".": "./src/index.js",
    "./protocol": "./src/protocol.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "lib",
    "README.md",
    "LICENSE",
    "dsh.patch.yml",
    "dsh.plugin.json"
  ],
  "dsh": {
    "bundle": {
      "patch": "./dsh.patch.yml"
    },
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-ui-settings-plugins",
        "@deepseek-ai/dsh-client-ui-settings",
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-ui-slots",
        "@deepseek-ai/dsh-client-ui-primitives"
      ]
    }
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@deepseek-ai/dsh-settings": "^0.1.1-rc.2",
    "@deepseek-ai/schemastery": "^3.18.1",
    "ws": "^8.18.0"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": ">=4.0.0",
    "@deepseek-ai/dsh-host-webserver": ">=0.1.1-rc.2"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
